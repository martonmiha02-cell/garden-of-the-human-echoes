# /fonts

Place `ProjektBlackbird.woff2` here.

The @font-face declaration in `styles.css` already points to:
  `/fonts/ProjektBlackbird.woff2`

If the file is absent the site falls back to Georgia, serif — fully functional.